name: Automatic Label Sync

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  label_sync:
    name: Label Sync
    uses: Jaronline/.github/.github/workflows/reusable-labelsync.yml@main
    with:
      repository: 'Jaronline/MiniMash'